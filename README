Added the ability to add a configuration file to the integration test assembly.

The application settings will override any matching application settings in the Web.config of the main ASP.NET MVC website that is being tested.